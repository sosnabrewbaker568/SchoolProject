// JavaScript code for the school project

const studentName = "John Doe";
const studentGrade = 8;
const studentSchool = "Elementary School";

function getStudentInfo() {
  console.log(`${studentName} is in ${studentGrade} grade at ${studentSchool}.`);
}

getStudentInfo();
